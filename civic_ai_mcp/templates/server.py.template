"""
{{name}} MCP server — {{description}}

Generated by `civic-ai-mcp init {{name}}` from civic-ai-mcp-toolkit.
"""

from __future__ import annotations

from typing import Any

from civic_ai_mcp import (
    configure_logging,
    create_mcp_server,
    error_envelope,
    run_main,
    traced,
)


mcp = create_mcp_server("{{name}}", default_port={{port}})


# ── Tools ───────────────────────────────────────────────────────────


@mcp.tool()
@traced("hello")
def hello(name: str = "world") -> dict[str, Any]:
    """
    Sample tool. Replace this with your real tool(s).

    Args:
      name: who to greet.

    Returns:
      {"greeting": "..."}
    """
    if not isinstance(name, str):
        return error_envelope("invalid_input", "name must be a string")
    return {"greeting": f"Hello, {name}!"}


# ── Entry point ─────────────────────────────────────────────────────


def main() -> None:
    configure_logging(logger_name="{{name}}_mcp")
    run_main(mcp)


if __name__ == "__main__":
    main()
